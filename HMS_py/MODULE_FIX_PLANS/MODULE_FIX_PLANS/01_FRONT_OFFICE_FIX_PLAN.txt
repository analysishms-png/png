[STATUS: COMPLETED AS OF 2026-09-19]
All unit tests pass. The Front Office module has been validated for guest profile update flow and checkout settlement accuracy.

FRONT OFFICE MODULE FIX PLAN
===========================

VB6 logic extracted from legacy flow:
- GuestProfile form updates GuestProf and also propagates changes into GuestFolio,
  RoomOcc, and PayCharge when the profile is revised.
- The VB6 flow treats GuestProf + GuestFolio + RoomOcc as the master chain for
  guest identity and stay data.
- FdCheckOut validates checkout-type logic from Enviro settings, compares room
  variation/checkout time values, and uses RoomOcc date/time structures.
- fdPaymentCharge handles receive and split settlement records in PayCharge.
- FdReSetlement and reverse checkout restore previous state by room and folio.

Scope:
- Guest profile
- Reservation / booking
- Check-in
- Room change
- Checkout / reverse checkout
- Folio / payment receive / settlement
- Guest history / room display

Primary Files:
- HMS_py/core/reservation.py
- HMS_py/core/checkin.py
- HMS_py/core/checkout.py
- HMS_py/core/folio.py
- HMS_py/core/roomstatus.py
- HMS_py/ui/frontoffice.py
- HMS_py/ui/checkout_ui.py
- HMS_py/ui/folio_ui.py
- HMS_py/ui/roomstatus_ui.py

Crystal / print pages and names:
- GuestProfile / GuestFolio / fdDisplayFolio / fdDisplayFolioSumm / fdPaymentCharge / fdPostChrg
- FdCheckOut / FdReSetlement / fdRoomOcc / FdRoomDisplay / HRoomCheckOutClearance
- RoomOccDisp / RoomOccupancyReportSummary / RoomOccupancyAnalysisReport
- Temp printing setup: report path, temp folder, printer name from Analysis.ini; print preview from VB6 form print module
- Crystal report family: rFomRepView, rRepHouse, rRepHousePreview, print/preview module names

VB6 behavior to preserve:
1. Guest profile change must cascade into GuestFolio and RoomOcc when the same guest is edited.
2. Room status is driven by RoomOcc and current occupancy state, not by guessed static values.
3. Check-out logic uses Enviro checkout type and room variation conditions before allowing final settlement.
4. Payment posting uses PayCharge with FolioNo, Vtype, VNo, GuestProf, and settlement flags.
5. Reverse checkout must restore room and folio state to the prior occupied/settled condition.

Known Missing / Risky Logic:
1. Guest profile validation is incomplete for duplicate mobile / identity entries.
2. Reservation to check-in handoff must confirm the live room occupancy state before opening folio.
3. Check-out must reconcile room charges, payment entries, and RoomOcc closure in one transaction.
4. Reverse checkout must restore occupancy status and remove stale charge assumptions.
5. Folio payment receive must validate pay code, available balance, and allowed modes.
6. Guest history must stay read-only and tied to real RoomOcc + GuestFolio rows.
7. Room status screen must use live availability states rather than guessed VB6 assumptions.

Required Fixes:
1. Add explicit validation for GuestProf and Booking inputs.
2. Ensure Booking -> CheckIn -> RoomOcc flow uses live DB state transitions.
3. Enforce check-out logic:
   - validate checkout type from Enviro
   - compute outstanding balance from PayCharge and GuestFolio
   - close only once the settlement state is valid
4. Enforce reverse checkout:
   - revert room status to occupied or vacant by original state
   - reverse only valid settlement records
5. Payment receive logic:
   - allow only known local pay types
   - reject unsupported pay codes with explicit error
   - log all entries to PayCharge with audit-safe values
6. Guest history: read only, no writes, no synthetic joins.
7. Room status: drive by RoomMast + RoomOcc + housekeeping flags.

Implementation Steps:
1. Validate Booking, GuestProf, GuestFolio, RoomOcc, RoomMast, PayCharge schema and lifecycle.
2. Confirm state transitions for CHK, RC, REC, and settlement.
3. Add focused regression tests for:
   - booking creation
   - check-in with valid room
   - outstanding balance before checkout
   - reverse checkout state restoration
4. Run smoke test login -> company -> main shell -> room status -> front desk.

Expected Result:
- Front office operations remain aligned to VB6 flow but with safer DB writes and validation.
- No stale assumptions from dead tables.
- All state transitions are traceable and testable.