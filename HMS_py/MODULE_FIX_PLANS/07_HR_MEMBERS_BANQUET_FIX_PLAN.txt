HR / MEMBERS / BANQUET FIX PLAN
==============================

Scope:
- HR masters
- Member category and facility masters
- Banquet / venue master screens
- Related setup screens without live operational tables

Primary Files:
- HMS_py/core/hr_masters.py
- HMS_py/core/members_masters.py
- HMS_py/core/banquet_masters.py
- HMS_py/ui/hr_members_ui.py
- HMS_py/ui/banquet_masters_ui.py

Crystal / print pages and names:
- prAttend / PrEmployee / PrHoliday / prSalCreate / PrCategoryMast / PrDesigMast
- Membership page names: MembershipMast / MemberModule / memCatMast / memFacilityMast / SmartCardMast / SmartCardRegistration
- Banquet page names: HallBooking / HallBill / CateringBooking / HallMenuCatalog / VenueMast / BanqAvailability / resBanq / FrmVenueMast / FrmVenueFeat
- Temp printing setup: member/banquet report preview and print queue using report path + temp folder from Analysis.ini
- Crystal report family: rMemberRepView / rHallRepView / report preview forms as applicable

Known Missing / Risky Logic:
1. Some member-related or banquet tables are not present in the live DB and must never be invented.
2. HR and member setups may exist as masters only, not as transaction flows.
3. Banquet operational tables like HallBooking/HallBill/EventMast are missing, so full transaction logic is blocked.
4. Screens should degrade gracefully to safe read-only viewer or no-op if DB tables are absent.

Required Fixes:
1. Keep master CRUD only for tables that are truly live.
2. Document blocked banquet transaction flows instead of building fake modules.
3. Validate category/facility codes against actual table rows.
4. Ensure UI navigation does not break when no transaction table exists.

Implementation Steps:
1. Recheck actual DB tables for members and banquet modules.
2. Distinguish MASTER vs TRANSACTION vs BLOCKED.
3. Add safe viewer/no-op behavior for missing operational tables.
4. Add regression tests verifying no crash when table absent.

Expected Result:
- HR and members modules remain working for actual database-backed masters.
- Banquet-related missing operations are clearly marked as blocked, not misrepresented as implemented.
- Shell remains stable even when a module is not operational in the current DB.

